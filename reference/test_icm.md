# Test the Model Output from a Stochastic Individual Contact Model

Tests whether the model output from an individual contact model is
consistent with key balancing equations for compartment and flow sizes
for each simulation for each time step.

## Usage

``` r
test_icm(x)
```

## Arguments

- x:

  An object of class `icm`.
