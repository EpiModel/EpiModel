# Test the Model Output from a Network Model

Tests whether the model output from a network model is consistent with
key balancing equations for compartment and flow sizes for each
simulation for each time step.

## Usage

``` r
test_net(x)
```

## Arguments

- x:

  An object of class `netsim`.
