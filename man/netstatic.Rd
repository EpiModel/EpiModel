% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/netstatic.R
\name{netstatic}
\alias{netstatic}
\title{Static Network Layer for Network Epidemic Models}
\usage{
netstatic(
  nw,
  group.attr = NULL,
  edgelist = NULL,
  arrivals = c("isolate", "new", "join"),
  arrivals.FUN = NULL
)
}
\arguments{
\item{nw}{An object of class \code{network} holding the node set and any vertex
attributes, as passed to \code{\link[=netest]{netest()}} for the dynamic layers of the
same model. The network must be undirected and not bipartite.}

\item{group.attr}{Name of a vertex attribute on \code{nw} whose values partition
the nodes into groups. Every pair of nodes sharing a non-missing
value becomes an edge, so each group is a clique. Nodes with a
missing (\code{NA}) value are isolates on this layer.}

\item{edgelist}{A two-column matrix or \code{data.frame} of node indices giving
the edges of the layer directly. Use this to carry a given set of
edges, such as an observed contact network, into the simulation
unchanged. It does not model how those edges arise; a network whose
ties depend on nodal and dyadic predictors is estimated with
\code{\link[=netest]{netest()}}.}

\item{arrivals}{Rule for wiring nodes that arrive during a simulation with
vital dynamics into the layer: \code{"isolate"} (the default; the new node
has no edges on this layer and its group attribute is \code{NA}), \code{"new"}
(each arrival starts a group of size one, with a fresh group id), or
\code{"join"} (each arrival joins an existing group and is connected to
all of its current members). See Details.}

\item{arrivals.FUN}{Optional function used with \code{arrivals = "join"} to
choose the group each arrival joins. See Details.}
}
\value{
An object of class \code{netstatic}, a list with elements:
\itemize{
\item \strong{newnetwork:} a \code{network} object holding the node set, the vertex
attributes of \code{nw}, and the static edges.
\item \strong{group.attr:} the grouping attribute name, or \code{NULL}.
\item \strong{arrivals}, \strong{arrivals.FUN:} the arrival rule.
\item \strong{target.stats}, \strong{target.stats.names:} the edge count of the layer,
named \code{"edges"}, so that the network statistics table printed by
\code{\link[=print.netsim]{print.netsim()}} and plotted by \code{\link[=plot.netsim]{plot.netsim()}} shows the layer's own
edge count as the target.
\item \strong{edapprox:} \code{FALSE}.
\item \strong{summary:} a list with the node count, edge count, mean degree,
number of isolates, and (with \code{group.attr}) the group size distribution.
}
}
\description{
Builds a contact layer with fixed edges, for structures that
are groups by definition (households, classrooms, hospital
wards, cabins) or for an observed contact network whose exact
edges are to be kept, that \code{\link[=netsim]{netsim()}} accepts anywhere in its
layer list alongside the layers estimated with \code{\link[=netest]{netest()}}. The
edges of a static layer never form or dissolve: there is no
formation model, no dissolution model, and no resimulation. It
is an addition to \code{netest} for cases where an ERGM is not
needed, not a substitute for one (see Details).
}
\details{
A \code{netest} object carries a formation model, a dissolution model, and a
starting network; \code{netsim} resimulates its layer each time step. That is
the right representation whenever tie existence is something to model: a
network with a degree distribution, mixing by attribute, and clustering to
reproduce is an ERGM's job whether its ties turn over quickly, slowly, or
not at all, and a long partnership duration in \code{\link[=dissolution_coefs]{dissolution_coefs()}} keeps
a \code{netest} layer close to fixed.

Some contact structures are groups by construction instead: every pair of
co-residents is a household contact for the whole simulation, every pair
of pupils in a classroom is a classroom contact, every pair of cabin-mates
shares a cabin. There is no tie-formation process to estimate, and an ERGM
can reproduce such cliques only through a \code{nodematch} term on the group
attribute targeted at its maximum, the number of within-group pairs. A
target at the maximum of a statistic has no finite coefficient, so the fit
runs to its iteration limit, the coefficient it stops at is arbitrary, and
a few groups are typically left incomplete. Before this function, the
alternative was to carry the clique edgelist into \code{netsim} as a parameter
and walk it in a custom infection module. \code{netstatic} is for these cases.
It builds a layer object that fills a slot in the layer list, is skipped
by the network resimulation and the edges correction, and is read by the
built-in infection modules through \code{\link[=discord_edgelist]{discord_edgelist()}} like any other
layer.

The layer is specified in one of three ways. With \code{group.attr}, all pairs of
nodes sharing a value of that attribute are connected (the household case).
With \code{edgelist}, the edges are given directly. With neither, the edges
already on \code{nw} are used, which is the observed-network case. In the first
two cases, any edges already on \code{nw} are discarded.

The group attribute must be an integer, numeric, or character vector; a
factor is refused because arrivals under \code{"new"} need to create values the
factor does not have. It should also be set on the network passed to
\code{\link[=netest]{netest()}} for the other layers, since \code{netsim} reads nodal attributes from
the first layer in its list; when it is not, \code{netsim} copies it from the
static layer.
}
\section{Arrivals}{

When a model has vital dynamics, each arriving node has to be placed on the
static layer. \code{netsim} applies the rule chosen with \code{arrivals} inside
\code{\link[=arrive_nodes]{arrive_nodes()}}, once per static layer, after the arrivals module has
created the node and set its other attributes:
\itemize{
\item \code{"isolate"}: the node gets no edges and its group attribute is \code{NA}. A
custom module may place it later.
\item \code{"new"}: the node gets a fresh group id, one larger than the largest
integer id in use (or a new unique string for character ids), and no
edges. Groups then grow only through further \code{"join"} arrivals, so this
rule suits models in which arrivals are single-person households.
\item \code{"join"}: the node joins an existing group and is connected to every
active member of that group, including other nodes joining the same
group in the same time step. By default the group is drawn with
probability proportional to its current size, which is the distribution
of a randomly chosen existing member. \code{arrivals.FUN} replaces that draw:
it is called as \code{arrivals.FUN(dat, at, new_ids, network)} and must return
one group id per element of \code{new_ids} (an \code{NA} leaves that node as an
isolate). Because the function receives the full \code{dat} object it can
read any nodal attribute; the example below places each newborn in a
household that already has a young child, and a function that reads the
group attribute back for \code{new_ids} lets a custom arrivals module set the
group itself.
}

The rule is applied to every static layer that has a \code{group.attr}. Layers
built from an \code{edgelist} or from the edges of \code{nw} support \code{"isolate"} only.
}

\section{Transmission over a static layer}{

The built-in infection modules treat every layer alike, so a static layer
transmits with the model's \code{inf.prob} and \code{act.rate} unless those are given
per layer as \code{\link[=multilayer]{multilayer()}} objects in \code{\link[=param.net]{param.net()}}:
\code{param.net(inf.prob = multilayer(0.45, 0.10), act.rate = multilayer(1, 2))}
with the household layer first and the community layer second. The
transmission matrix records the layer of each transmission in its
\code{network} column.

Nodes departing the population are removed from a static layer with their
edges, as on every other layer. Network statistics for the layer are
recorded through \code{nwstats.formula} in \code{\link[=control.net]{control.net()}}, with the default
\code{"formation"} meaning \code{~edges} for a static layer, and the cumulative
edgelist records its edges with a start time of 0. \code{\link[=netdx]{netdx()}} refuses a
\code{netstatic} object, since there is no model to diagnose; the group size
distribution and mean degree are shown by \code{print}.
}

\examples{
\dontrun{
# Households as cliques under a community TERGM layer
hh <- sample_groups(500, c("adult" = 0.3, "adult adult" = 0.4,
                           "adult adult child" = 0.2,
                           "adult adult child child" = 0.1),
                    attr.name = "age")
nw <- network_initialize(n = 500)
nw <- set_vertex_attribute(nw, "age", hh$age)
nw <- set_vertex_attribute(nw, "hh_id", hh$group)

est_hh <- netstatic(nw, group.attr = "hh_id", arrivals = "join")
est_hh
print(est_hh, by = "age")

est_com <- netest(nw, formation = ~edges + nodematch("age"),
                  target.stats = c(250, 175),
                  coef.diss = dissolution_coefs(~offset(edges), 20),
                  verbose = FALSE)

param <- param.net(inf.prob = multilayer(0.3, 0.05), act.rate = 1)
init <- init.net(i.num = 10)
control <- control.net(type = "SI", nsteps = 50, nsims = 1,
                       tergmLite = TRUE, resimulate.network = TRUE,
                       verbose = FALSE)
sim <- netsim(list(est_hh, est_com), param, init, control)

# Share of transmissions on the household layer
tm <- get_transmat(sim)
mean(tm$network == 1)

# Newborns join a household that already has a child
est_hh2 <- netstatic(nw, group.attr = "hh_id", arrivals = "join",
  arrivals.FUN = function(dat, at, new_ids, network) {
    hh <- get_attr(dat, "hh_id")
    age <- get_attr(dat, "age")
    pool <- hh[which(age == "child" & !is.na(hh))]
    pool[sample.int(length(pool), length(new_ids), replace = TRUE)]
  })
}

}
\seealso{
\code{\link[=sample_groups]{sample_groups()}} builds a population from a table of group
types, and \code{\link[=assign_groups]{assign_groups()}} assigns group ids to an existing population
with composition constraints. \code{\link[=netsim]{netsim()}} runs the simulation, and
\code{\link[=multilayer]{multilayer()}} sets per-layer parameters and controls.
}
