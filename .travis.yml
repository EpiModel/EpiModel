language: r
sudo: required

warnings_are_errors: true

r_packages:
 - covr
r_binary_packages:
 - httpuv
 - stringi

after_success:
  - Rscript -e 'covr::coveralls()'

notifications:
  email:
    on_success: never
    on_failure: change

env:
  - global:
      - NOT_CRAN=true
